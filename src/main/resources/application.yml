spring:
  jpa:
    database: MYSQL
    hibernate:
      dialect: org.hibernate.dialect.MySQLInnoDBDialect
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        globally_quoted_identifiers: true

  datasource:
    url: jdbc:mysql://localhost:3306/meals?useSSL=false
    username: user
    password: pass1
    driver-class-name: com.mysql.cj.jdbc.Driver

